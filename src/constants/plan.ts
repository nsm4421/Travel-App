export enum BudgetType {
  CHEAP = "cheap",
  MODERATE = "moderate",
  LUXURY = "luxury",
}

export enum AccompayType {
  SOLO = "alone",
  COUPLE = "couple",
  FAMILY = "family",
  FRIEND = "friend",
}
